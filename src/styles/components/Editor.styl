.editor
  border-top 1px dashed alpha(black, 0.1)
  padding 10px 20px

  overflow-y auto
  overflow -moz-scrollbars-none
  -ms-overflow-style none

  flex-grow 1

  &::-webkit-scrollbar
    width 0 !important
